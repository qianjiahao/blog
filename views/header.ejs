<! DOCTYPE html>
<html xmlns="http://www.w3.org/1999/html">
<head>
    <meta charset="utf-8"/>
    <title>blog2</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
<header>
    <h1><%= title %></h1>
    <% if(user){%>
    <a href="/u/<%= user.name%>"><div class="head-name"><%= user.name%></div><img width="100" class="head" src="/images/head-images/<%= user.head%>.jpg"></a>
    <%}%>
</header>

<nav>
    <span><a href="/" title="home">home</a></span>
    <span><a href="/archive" title="archive">archive</a></span>
    <span><a href="/tags" title="tags">tags</a></span>
    <span><a href="/links" title="links">links</a></span>
    <% if(user){ %>
    <span><a href="/post" title="post">post</a></span>
    <span><a href="/upload" title="upload">upload</a></span>
    <span><a href="/logout" title="logout">logout</a></span>
    <% }else{ %>
    <span><a href="/login" title="login">login</a></span>
    <span><a href="/reg" title="register">register</a></span>
    <% } %>
    <span>
    <form action="/search" method="GET">
        <input type="text" name="keyword" placeholder="SEARCH" class="search">
    </form>
</span>
</nav>
<article>
    <% if(success){ %>
    <div><%= success %></div>
    <% }else{ %>
    <div><%= error %></div>
<% } %>